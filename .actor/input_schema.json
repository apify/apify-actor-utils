{
    "title": "safePushData demo input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "strategy": {
            "title": "Cleaning strategy",
            "type": "string",
            "description": "How safePushData handles items that fail schema validation. 'drop' removes the invalid item from the batch. 'cleanFields' tries to delete the offending field(s) and retry, falling back to dropping when the item cannot be salvaged.",
            "editor": "select",
            "enum": ["drop", "cleanFields"],
            "default": "drop"
        }
    }
}
